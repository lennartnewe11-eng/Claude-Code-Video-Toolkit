# Heavy binaries / media — regenerated by setup, not versioned
tools/ffmpeg
tools/ffprobe
footage/*
renders/*
!footage/.gitkeep
!renders/.gitkeep
*.mov
*.mp4

# Secrets — never commit API keys
.env
*.env
tools/secrets.env
