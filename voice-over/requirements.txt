requests>=2.31
# Optional: liefert ein ffmpeg-Binary fuer sauberes Zusammenfuegen der Chunks
# (ohne System-Installation). Wird automatisch genutzt, wenn vorhanden.
imageio-ffmpeg>=0.4.9
# Nur fuer extract_narration.py (Text/Erzaehler-Passagen aus Skript-PDFs ziehen).
pymupdf>=1.24
